using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LedgerController : MonoBehaviour
{
    //Stores the ledger list where all the labelling happens
    public List<LabelModel> LedgerLabelList;

    //(might be good to use drop downs for the ledger UI options)

    
    void Start()
    {
        
    }

    
    void Update()
    {
        
    }

    //eventually, when you check whether people inside a memory are all labelled, call GameplayManager.UploadMemory()
}
